# 1. Next.js App Router — Dynamic Routes (bloods/[id], dashboard, etc.)
/bloods/:id          /bloods/[id]           200
/dashboard           /dashboard             200
/about               /about                 200
/profile             /profile               200

# 2. Clerk Authentication — Important!
# These must be 200! (not 301) so Clerk can handle auth properly
/sign-in             /sign-in               200
/sign-up             /sign-up               200
/user                /user                  200

# 3. SPA Fallback — Catch-all for client-side routing
/*                   /index.html            200